import {FatoryCreateManagmentDashboard} from './dashboard.min';

$(document).ready(function () {
    let config = {
        company_id : $("#company_id").html(),
        user_id : $("#user_id").html(),
        type_user : "COMPANY",
    };
    FatoryCreateManagmentDashboard(config);
});